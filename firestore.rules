rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // The household document is shared by everyone with an account in this
    // project, which is the intended model: a couple's shared planner.
    match /households/{houseId} {
      allow read, write: if request.auth != null;
    }
  }
}
